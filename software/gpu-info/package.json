{
  "name": "@platforma-open/milaboratories.gpu-test.gpu-info",
  "version": "3.0.1",
  "description": "GPU detection and info reporting",
  "files": [
    "./dist/**/*"
  ],
  "type": "module",
  "scripts": {
    "build": "pl-pkg build && node ./scripts/inject-nvidia-env.mjs",
    "prepublishOnly": "pl-pkg prepublish",
    "do-pack": "shx rm -f *.tgz && pnpm build && pnpm pack && shx mv platforma-open*.tgz package.tgz",
    "changeset": "changeset",
    "version-packages": "changeset version"
  },
  "dependencies": {},
  "devDependencies": {
    "@platforma-open/milaboratories.runenv-python-3": "catalog:",
    "@platforma-sdk/package-builder": "catalog:"
  },
  "block-software": {
    "entrypoints": {
      "main": {
        "binary": {
          "artifact": {
            "type": "python",
            "registry": "platforma-open",
            "environment": "@platforma-open/milaboratories.runenv-python-3:3.12.10-rapids",
            "//docker-base-image": "@platforma-open/milaboratories.runenv-python-3:3.12.10-rapids",
            "dependencies": {
              "toolset": "pip",
              "requirements": "requirements.txt"
            },
            "root": "./src"
          },
          "cmd": [
            "python",
            "{pkg}/main.py"
          ]
        }
      }
    }
  }
}
