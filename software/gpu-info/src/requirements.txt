# No hard dependencies — GPU libraries are detected at runtime.
# CuPy and numpy are available in RAPIDS/CUDA runtime environments;
# on systems without them, the script falls back to nvidia-smi detection.
